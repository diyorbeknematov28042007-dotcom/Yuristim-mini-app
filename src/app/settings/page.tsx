"use client";
import { Bell, MonitorSmartphone, ShieldCheck } from "lucide-react"; import { PageHeader } from "@/components/ui/page-header"; import { InfoCard } from "@/components/ui/info-card"; import { useI18n } from "@/lib/i18n/context"; import { useTelegram } from "@/lib/telegram/context";
export default function SettingsPage() { const { t } = useI18n(); const telegram = useTelegram(); const rows = [{ icon: Bell, label: t("settings.notifications") }, { icon: ShieldCheck, label: t("settings.security") }, { icon: MonitorSmartphone, label: t("settings.browserMode") }]; return <><PageHeader title={t("settings.title")} subtitle={t("settings.subtitle")} /><div className="app-content-width px-4 py-5 sm:px-5"><InfoCard className="divide-y divide-neutral-100 p-0">{rows.map(({ icon: Icon, label }, index) => <div key={label} className="flex min-h-14 items-center gap-3 px-4"><Icon size={19} strokeWidth={1.8} className="text-neutral-600" /><span className="flex-1 text-sm font-medium text-ink">{label}</span><span className="text-xs text-muted">{index === 2 ? (telegram.isTelegram ? "Telegram" : "Browser") : t("common.soon")}</span></div>)}</InfoCard></div></>; }
