"use client";
import { Gauge, Sparkles, WalletCards } from "lucide-react"; import { useMiniApp } from "@/features/app-state/context"; import { useI18n } from "@/lib/i18n/context"; import { PageHeader } from "@/components/ui/page-header";
export default function UsagePage() { const { usage } = useMiniApp(); const { t } = useI18n(); const items = [{ label: t("usage.balance"), value: usage?.balance ?? 0, icon: Gauge }, { label: t("usage.weekly"), value: usage?.weeklyCredits ?? 0, icon: Sparkles }, { label: t("usage.permanent"), value: usage?.permanentCredits ?? 0, icon: WalletCards }]; return <><PageHeader title={t("usage.title")} subtitle={t("usage.subtitle")} /><div className="app-content-width px-4 py-5 sm:px-5"><div className="divide-y divide-neutral-100 border-y border-neutral-100">{items.map(({ label, value, icon: Icon }) => <div key={label} className="flex min-h-16 items-center gap-3 py-3"><Icon size={20} strokeWidth={1.8} className="text-yuristim-dark" /><span className="flex-1 text-sm font-medium text-ink">{label}</span><span className="text-sm font-semibold tabular-nums text-ink">{value.toFixed(1)}</span></div>)}</div></div></>; }
