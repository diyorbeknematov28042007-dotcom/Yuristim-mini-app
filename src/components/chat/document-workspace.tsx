"use client";
import { FileText, Upload } from "lucide-react"; import { useState } from "react"; import { useI18n } from "@/lib/i18n/context"; import { EmptyState } from "@/components/ui/empty-state"; import { PrimaryButton } from "@/components/ui/primary-button"; import { UploadSheet, type AttachmentKind } from "./upload-sheet";
export function DocumentWorkspace() { const { t } = useI18n(); const [sheetOpen, setSheetOpen] = useState(false); const [feedback, setFeedback] = useState<string | null>(null); const onSelect = (kind: AttachmentKind) => { setFeedback(`${t("attachment.demo")} · ${kind}`); setSheetOpen(false); }; return <div className="flex min-h-full items-center py-10 sm:py-14"><EmptyState icon={<FileText size={23} strokeWidth={1.8} />} title={t("document.emptyTitle")} body={t("document.emptyBody")} actions={<div className="mx-auto max-w-[320px]"><PrimaryButton onClick={() => setSheetOpen(true)} className="w-full"><Upload size={18} strokeWidth={1.9} />{t("document.upload")}</PrimaryButton><p className="mt-3 text-xs text-muted">{t("document.formats")}</p>{feedback ? <p className="mt-2 text-xs text-yuristim-dark">{feedback}</p> : null}</div>} /><UploadSheet open={sheetOpen} onClose={() => setSheetOpen(false)} onSelect={onSelect} /></div>; }
