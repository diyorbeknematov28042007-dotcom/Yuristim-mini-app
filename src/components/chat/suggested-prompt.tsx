import { ArrowUpRight } from "lucide-react"; export function SuggestedPrompt({ text, onClick }: { text: string; onClick: () => void }) { return <button type="button" onClick={onClick} className="group flex min-h-11 w-full items-center justify-between gap-3 border-b border-neutral-100 py-2.5 text-left text-sm text-ink transition-colors last:border-b-0 hover:text-yuristim"><span>{text}</span><ArrowUpRight size={16} strokeWidth={1.8} className="shrink-0 text-neutral-300 transition-colors group-hover:text-yuristim" /></button>; }
