"use client";
import { Camera, FileUp, Mic } from "lucide-react"; import { BottomSheet } from "@/components/ui/bottom-sheet"; import { useI18n } from "@/lib/i18n/context"; export type AttachmentKind = "file" | "camera" | "voice";
export function UploadSheet({ open, onClose, onSelect }: { open: boolean; onClose: () => void; onSelect: (kind: AttachmentKind) => void }) { const { t } = useI18n(); const options = [{ id: "file" as const, label: t("attachment.file"), icon: FileUp }, { id: "camera" as const, label: t("attachment.camera"), icon: Camera }, { id: "voice" as const, label: t("attachment.voice"), icon: Mic }]; return <BottomSheet open={open} title={t("attachment.title")} onClose={onClose}><div className="space-y-1 pt-1">{options.map(({ id, label, icon: Icon }) => <button key={id} type="button" onClick={() => onSelect(id)} className="flex min-h-12 w-full items-center gap-3 rounded-[13px] px-3 text-left text-sm font-medium text-ink transition-colors hover:bg-neutral-50"><span className="flex size-9 items-center justify-center rounded-[11px] bg-neutral-100 text-neutral-700"><Icon size={19} strokeWidth={1.8} /></span>{label}</button>)}<p className="px-3 pb-1 pt-3 text-xs leading-5 text-muted">{t("attachment.note")}</p></div></BottomSheet>; }
