import { MessageSquareText } from "lucide-react"; import { cn } from "@/lib/utils/cn"; import type { ChatHistoryItem as ChatHistoryItemType } from "@/types/domain";
export function HistoryItem({ item, active, onClick }: { item: ChatHistoryItemType; active: boolean; onClick: () => void }) { return <button type="button" onClick={onClick} className={cn("flex min-h-11 w-full items-center gap-3 rounded-[12px] px-3 text-left transition-colors", active ? "bg-mint-soft text-yuristim-dark" : "text-ink hover:bg-neutral-100")}><MessageSquareText size={18} strokeWidth={1.8} className="shrink-0 opacity-75" /><span className="min-w-0 flex-1 truncate text-sm font-medium">{item.title}</span></button>; }
