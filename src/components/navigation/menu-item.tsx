import Link from "next/link"; import type { LucideIcon } from "lucide-react"; import type { ReactNode } from "react";
export function MenuItem({ href, icon: Icon, label, trailing, onNavigate }: { href: string; icon: LucideIcon; label: string; trailing?: ReactNode; onNavigate?: () => void }) { return <Link href={href} onClick={onNavigate} className="flex min-h-11 items-center gap-3 rounded-[12px] px-3 text-sm font-medium text-ink transition-colors hover:bg-neutral-100 active:bg-neutral-100"><Icon size={19} strokeWidth={1.8} className="shrink-0 text-neutral-600" aria-hidden="true" /><span className="min-w-0 flex-1 truncate">{label}</span>{trailing}</Link>; }
