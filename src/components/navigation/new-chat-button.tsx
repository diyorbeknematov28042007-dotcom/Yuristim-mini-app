import { Plus } from "lucide-react"; export function NewChatButton({ onClick, label }: { onClick: () => void; label: string }) { return <button type="button" onClick={onClick} className="flex min-h-11 w-full items-center justify-center gap-2 rounded-[13px] border border-neutral-300 bg-white px-3 text-sm font-semibold text-ink transition-colors hover:bg-neutral-50 active:bg-neutral-100"><Plus size={19} strokeWidth={2} />{label}</button>; }
