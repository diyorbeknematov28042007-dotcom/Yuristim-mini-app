"use client";
import { ArrowLeft, Menu } from "lucide-react"; import { useRouter } from "next/navigation"; import { useCallback } from "react"; import { useMiniApp } from "@/features/app-state/context"; import { useI18n } from "@/lib/i18n/context"; import { useTelegramBackButton } from "@/hooks/use-telegram-back-button"; import { IconButton } from "./icon-button";
export function PageHeader({ title, subtitle }: { title: string; subtitle?: string }) { const router = useRouter(); const { openDrawer } = useMiniApp(); const { t } = useI18n(); const goBack = useCallback(() => router.push("/"), [router]); useTelegramBackButton(true, goBack); return <header className="app-safe-top border-b border-neutral-100 bg-white"><div className="app-content-width px-2 pb-3 sm:px-4"><div className="flex min-h-12 items-center gap-1"><IconButton aria-label={t("common.back")} onClick={goBack}><ArrowLeft size={21} strokeWidth={1.9} /></IconButton><div className="min-w-0 flex-1 px-1"><h1 className="truncate text-base font-semibold text-ink">{title}</h1></div><IconButton aria-label="Menu" onClick={openDrawer}><Menu size={21} strokeWidth={1.9} /></IconButton></div>{subtitle ? <p className="px-3 text-sm leading-5 text-muted">{subtitle}</p> : null}</div></header>; }
