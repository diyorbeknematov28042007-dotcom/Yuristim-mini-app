import { ChevronRight, type LucideIcon } from "lucide-react";
import type { ReactNode } from "react";
export function ListItem({ icon: Icon, title, description, trailing }: { icon: LucideIcon; title: string; description?: string; trailing?: ReactNode }) { return <div className="flex min-h-16 items-center gap-3 border-b border-border py-3 last:border-b-0"><div className="flex size-10 shrink-0 items-center justify-center rounded-[13px] bg-mint-soft text-yuristim"><Icon className="size-5" aria-hidden="true" /></div><div className="min-w-0 flex-1"><p className="text-sm font-semibold text-ink">{title}</p>{description ? <p className="mt-0.5 truncate text-xs text-muted">{description}</p> : null}</div>{trailing ?? <ChevronRight className="size-4 text-muted" aria-hidden="true" />}</div>; }
